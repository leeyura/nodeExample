doctype html
html
   head
     meta(charset='utf-8')
   body
     h1
       a(href='/main') Server Side JavaScript
     ul
       each topic in topics
         li
           a(href='/title/'+topic.id)= topic.title
     artcle
       h1= 'Delete? '+topic.title
       form(action='/main/'+topic.id+'/delete' method='post')
         p
           input(type='submit' value='YES')
       a(href='/title/'+topic.id) NO